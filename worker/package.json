{
  "name": "proper-holdings-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute proper-holdings-booking --file=schema.sql",
    "db:init:remote": "wrangler d1 execute proper-holdings-booking --remote --file=schema.sql",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
